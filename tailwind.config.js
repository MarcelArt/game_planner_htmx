/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './views/src/**/*.{html,js,css}', // Scan all HTML, JS, and CSS files in the views/src directory and its subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

